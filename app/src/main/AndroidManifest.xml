<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nammamela">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Namma Mela"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NammaMela">

        <activity
            android:name=".BookingSuccessActivity"
             />

        <activity
            android:name=".CastActivity"
            android:exported="false" />

        <activity
            android:name=".DramaDetailsActivity"
            android:exported="false" />

        <activity
            android:name=".FanWallActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".ShowListActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>